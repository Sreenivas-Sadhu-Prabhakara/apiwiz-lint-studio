openapi: 3.0.0
info:
  title: Orders API
  version: v1
  contact:
    name: Orders Team
paths:
  /Order_Items:
    get:
      operationId: listOrderItems
      responses:
        "200":
          description: A list of order items
